using UnityEngine;
using System.Collections;

public class StateMachine : MonoBehaviour {
    /*
    * State machine needs States to work with
    * Also needs Interface
    * This is used so that we do not need a few huge files, but instead many small files.
    * Main State
    * Pause State
    * Play State
    * Options State
    * Level State
    */
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
